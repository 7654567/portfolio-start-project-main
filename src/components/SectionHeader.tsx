import React from 'react';
import styled from "styled-components";

// type SectionHeaderPropsTypes = {}
export const SectionHeader = (props:any) => {
	console.log(props)
	return (
		<SteyledSectionHeader>

		</SteyledSectionHeader>
	);
};

const SteyledSectionHeader = styled.h2`
	
`

